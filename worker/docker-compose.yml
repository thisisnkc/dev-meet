version: "3.8"

services:
  worker:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    restart: unless-stopped
    network_mode: "host"
    volumes:
      - .env:/app/.env
